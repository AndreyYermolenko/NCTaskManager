	TaskManager -- это учебный проект, выполненный под кураторством сотрудников компании NetCracker, который реализует "ежедневник". 
	ѕрограмма позвол€ет/имеет:
+создавать/удал€ть/измен€ть/просматривать задачи;
+отслеживать в фоновом режиме задачи, которые готов€тс€ к выполнению;
+сохран€ет/восстанавливает список задач при запуске/завершении программы;
+имеет пон€тный и удобный консольный интерфейс;

	¬ рамках проекта использовались принципы/технологии/реализации:
+паттерн MVC;
+принцип Inversion of Control;
+собственные реализации ArrayList и LinkedList, обладающие всем необходимым функционалом дл€ работы с массивом задач (в т.ч. имплементируют Interable, Comparable, Clonable);
+многопоточность дл€ отслеживани€ времени выполнени€ задач в фоновом режиме;
+сериализаци€/десериализаци€ списка задач (в байтовом виде или в формате Json);
+логгирование с помощью библиотеки log4j 2;
+пон€тный нейминг полей, классов, методов;
+документаци€ JavaDoc к проекту;
